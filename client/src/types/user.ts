export type User = { 
    id: string,
    username: string,
    email: string,
    phoneNumber: string,
    address: string,
    createdAt: string,
    updatedAt: string 
};

export type Form = {
    email: string;
    password: string;
  };

